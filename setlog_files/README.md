**This folder contains setlog files that are using acrross the project.**
Note1: `setlog.pl` is splited into setlog_linprog.pl and setlog_clpq.pl versions. The only difference between them is the library they are using. 
Note2: Inside the project each version renames back to setlog. This is done by compilators. 